# Minimal Air config for Go hot reload
root = "."
[build]
  cmd = "go build -o ./tmp/main ."
  bin = "./tmp/main"
  include_ext = [".go", ".tpl", ".tmpl", ".html"]
  exclude_dir = ["vendor", "tmp"]
  delay = 1000
[log]
  level = "info" 